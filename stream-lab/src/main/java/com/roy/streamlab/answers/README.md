# 参考答案

建议按下面的顺序使用答案：

1. 先阅读 `docs/guides/STREAM_PROGRAMMING_GUIDE.md` 中对应 Lab。
2. 独立完成 `exercises` 包中的代码。
3. 运行 `StreamExerciseCheck` 检查结果。
4. 仍然没有思路时，先看 `docs/hints` 中的分级提示。
5. 最后再对照本目录中的参考实现。

答案不是唯一写法。只要结果、顺序和题目要求一致，就可以保留自己的实现。

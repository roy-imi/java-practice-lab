# Stream Lab 04 提示

## 提示 1

求和链条：

```text
filter -> mapToInt -> sum
```

## 提示 2

判断“是否存在”使用 `anyMatch`。Lambda 应在遇到未支付订单时返回 true。

## 提示 3

查找链条：

```text
filter -> findFirst
```

`findFirst` 已经返回 Optional，不需要自己包装。

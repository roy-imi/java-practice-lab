# 第 4 课提示

## 提示 1：先找依赖箭头

只有“折扣查询”需要先知道用户等级。价格和运费不依赖它，应该尽早并行启动。

## 提示 2：避免两层 Future

`thenApply(level -> supplyAsync(...))` 会得到两层 Future。改用 `thenCompose`，让结果保持为一层 `CompletableFuture<Integer>`。

## 提示 3：分两次合并

可以先把价格与折扣合并成一个很小的中间值，再把它与运费合并。中间值可以使用两个元素的 `int[]`，也可以自己增加一个语义更好的小类。

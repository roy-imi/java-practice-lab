# 第 3 课提示

## 提示 1：每一阶段只做一件事

先让 `supplyAsync` 得到原价整数，再用两个 `thenApply` 分别做“计算”和“展示”。不要把三个动作都塞进一个大 lambda。

## 提示 2：金额

服务返回单位是分。九折价可用整数运算 `priceCents * 90 / 100`。格式化工具已经提供：`Money.format(...)`。

## 提示 3：消费结果

`thenAccept` 接收 `Consumer<T>`，它只消费结果而不产生新值，因此返回阶段的泛型参数是 `Void`。

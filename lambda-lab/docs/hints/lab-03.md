# 第 3 课提示

## 提示 1：先写普通 Lambda

卡住时先写等价 Lambda，例如“输入商品，返回商品名称”。确认参数和返回值后，再观察 Lambda 是否只调用了一个已有方法。

## 提示 2：对象是否已经确定

- 列表 `target` 已经存在，因此使用“已有对象::实例方法”。
- 商品对象要等 Function 被调用时才传入，因此使用“类名::实例方法”。

## 提示 3：构造器

`Supplier.get()` 没有参数，所以应引用 `ArrayList` 的无参数构造器。不要提前创建并反复返回同一个列表。
